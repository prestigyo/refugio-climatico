name: Backfill histórico AEMET

on:
  workflow_dispatch:
    inputs:
      anios:
        description: "Años hacia atrás a descargar"
        required: true
        default: "1"
        type: string

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Instalar dependencias
        run: pip install -r aemet-temperaturas/requirements.txt

      - name: Configurar git
        run: |
          git config --global user.name  "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Backfill anio por anio (commit incremental)
        working-directory: aemet-temperaturas
        env:
          AEMET_API_KEY: ${{ secrets.AEMET_API_KEY }}
        run: |
          set -e
          ANIOS=${{ inputs.anios }}
          ANIO_BASE=$(date -u +%Y)
          for i in $(seq 0 $((ANIOS - 1))); do
            ANIO=$((ANIO_BASE - i))
            echo "===== Anio $ANIO ====="
            python scripts/backfill_historico.py \
              --desde "$ANIO-01-01" \
              --hasta "$ANIO-12-31"
            cd ..
            git add aemet-temperaturas/datos/
            if ! git diff --cached --quiet; then
              git commit -m "Backfill anio $ANIO"
              git push
            else
              echo "Sin cambios en $ANIO"
            fi
            cd aemet-temperaturas
          done
